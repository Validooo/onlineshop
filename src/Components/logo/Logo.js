import React from 'react';
import { Link, useLocation } from 'react-router-dom';
import { Button } from '@mui/material';
import logoo from '../../Pictures/logoo.png'


export default function Logo() {
  return (
    <div>

      {/*
      <Button component={Link} to="/"
        className='logopic'        >
        <img src={logoo} alt="" />
  
      </Button>
  */}
    </div>
  )
}
